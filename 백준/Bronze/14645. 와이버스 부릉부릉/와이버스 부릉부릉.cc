#include <iostream>

using namespace std;

int main()
{
    int N, M;
    cin >> N >> M;
    
    int in;
    for (int i = 0; i < N * M; ++i)
        cin >> in;
    
    cout << "비와이";
    
    return 0;
}